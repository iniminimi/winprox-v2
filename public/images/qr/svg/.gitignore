*.log
.DS_Store
Thumbs.db

.env
.env.backup
.env.production

.phpactor.json
.phpunit.result.cache
/.phpunit.cache
/auth.json

/.fleet
/.idea
/.nova
/.vscode
/.zed

/node_modules
/vendor
# public/build: commit after `npm run build` so production servers without Node get CSS/JS
/public/hot
/public/storage

# Laravel runtime files (keep only placeholder .gitignore files)
# Large local datasets (e.g. KBO imports) live under storage/app/ — never commit.
/storage/app/kbo/

# Local KBO CSV extract at repo root (never commit).
/kbo/
/storage/**
!/storage/.gitignore
!/storage/app/.gitignore
!/storage/app/private/.gitignore
!/storage/app/public/.gitignore
!/storage/framework/.gitignore
!/storage/framework/cache/.gitignore
!/storage/framework/cache/data/.gitignore
!/storage/framework/sessions/.gitignore
!/storage/framework/testing/.gitignore
!/storage/framework/views/.gitignore
!/storage/logs/.gitignore

# Local/system folders (machine specific)
/.cagefs/
/.cl.selector/

# Local debug/export artifacts
/fortify-debug/
/fortify-debug.zip
/winprox*.zip
*.sql.gz

Homestead.json
Homestead.yaml
