{
    "private": true,
    "type": "module",
    "scripts": {
        "build": "php artisan view:cache && vite build",
        "dev": "vite",
        "prepare": "node scripts/install-git-hooks.mjs",
        "fix:locales": "node scripts/locales.mjs fix",
        "check:locales": "node scripts/locales.mjs check",
        "check:locales:parity": "node scripts/locales.mjs parity",
        "check:bom": "node scripts/check-bom.mjs",
        "check:architecture": "node scripts/check-architecture.mjs",
        "capture:manual": "npm install --prefix scripts/capture-pkg && node scripts/capture-manual-screenshots.mjs"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "axios": "^1.7.4",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^1.2.0",
        "tailwindcss": "^4.0.0",
        "vite": "^6.0.11"
    },
    "dependencies": {
        "@fontsource/inter": "^5.2.8",
        "quill": "^2.0.3"
    }
}
